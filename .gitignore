# Dépendances Node (étape 2+)
node_modules/

# Secrets — ne jamais committer (DB, JWT, ADMIN_EMAILS, mots de passe)
.env
.env.*
!.env.example
!**/.env.example
backend/.env
**/.env
!backend/.env.example

# Images uploadées par les restaurants
backend/uploads/

# Sauvegardes automatiques (données clients — ne pas committer)
backups/*
!backups/.gitkeep

# Assets de démo locaux (non nécessaires en production)
dossier de resto/

# OS
Thumbs.db
.DS_Store
.tmp.driveupload/
