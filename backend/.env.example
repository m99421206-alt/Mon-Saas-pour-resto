# MenuGo — variables d'environnement (copier vers .env)
# cp .env.example .env   puis renseigner les valeurs

# --- Environnement ---
NODE_ENV=development
PORT=4000
HOST=0.0.0.0

# --- MySQL ---
DB_HOST=127.0.0.1
DB_USER=
DB_PASSWORD=
DB_NAME=MenuGo
DB_CONNECTION_LIMIT=10

# --- Authentification ---
# Générer une clé longue aléatoire en production (ex. openssl rand -hex 32)
JWT_SECRET=changez_moi_cle_longue_aleatoire
JWT_EXPIRES_IN=7d
BCRYPT_SALT_ROUNDS=10

# --- Administration plateforme ---
# Emails autorisés sur /api/admin/* (séparés par des virgules)
# OBLIGATOIRE en production (NODE_ENV=production)
ADMIN_EMAILS=admin@votredomaine.com

# --- CORS ---
# URL exacte du frontend (sans slash final). Plusieurs origines : séparer par des virgules.
# Ex. https://app.menugo.com ou http://localhost:5500 en dev
CORS_ORIGIN=http://localhost:5500

# --- Affichage console au démarrage ---
LAN_URL=http://localhost:4000

# --- Abonnements (migration optionnelle) ---
TRIAL_BACKFILL_DAYS=14
