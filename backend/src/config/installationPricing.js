/**
 * Tarif installation clé en main (landing + revenus admin CRM).
 * Ne pas confondre avec l'abonnement mensuel (platformSettings).
 */

"use strict";

var INSTALLATION_PRICE_CFA = 35000;
var INSTALLATION_PRICE_CURRENCY = "FCFA";

module.exports = {
  INSTALLATION_PRICE_CFA: INSTALLATION_PRICE_CFA,
  INSTALLATION_PRICE_CURRENCY: INSTALLATION_PRICE_CURRENCY,
};
